{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "preDeployCommand": "php artisan migrate --force && php artisan db:seed --force",
    "startCommand": "php artisan storage:link || true && php artisan serve --host=0.0.0.0 --port=$PORT"
  }
}
